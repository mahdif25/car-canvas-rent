

# Move Sticky Buttons to Left Side in Fleet Editor

## Change
In `src/pages/admin/AdminFleet.tsx`, change the sticky footer button alignment from `justify-end` to `justify-start` so the Save and Return buttons appear on the left side instead of the right.

## Files Modified
- `src/pages/admin/AdminFleet.tsx` — change `justify-end` to `justify-start` in the sticky footer's flex container

